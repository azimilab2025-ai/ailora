# Disabled Oya safety qualification

Status: locally qualified boundary; live provider remains an external activation gate.

Oya remains provider-neutral, disabled by default, non-networked, non-billable, and advisory-only. The policy rejects prompt injection, false authority, tool execution, and exhausted input budgets. Safe input deterministically falls back to text chat. This command neither installs a vendor SDK nor uses credentials. The existing commercial arrangement does not change these engineering gates; later activation remains possible after project completion, qualified real-data integration, vendor documentation, explicit authorization, and production safety review.
