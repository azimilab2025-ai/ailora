# Performance, capacity, and FinOps qualification

The local contract records p50, p95, p99 latency, throughput, memory, CPU, query count, artifact size, environment metadata, and a deterministic dataset digest. Explicit thresholds fail closed. Evidence generated here is a reproducible qualification fixture, not production capacity or soak evidence. Paid workloads, vendor capacity claims, and production SLO claims remain external gates.
